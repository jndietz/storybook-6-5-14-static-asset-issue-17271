import { Route, Router } from "react-router";
import SampleComponent from "../SampleComponent/SampleComponent";

export default (props) => {
  <Router>
    <SampleComponent name="Marty" />
  </Router>;
};
